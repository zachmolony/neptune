import PropTypes from "prop-types";

const UnshippedOrders = ({ unshippedOrders }) => {
  return <>unshippedOrders</>;
};

UnshippedOrders.propTypes = {
  unshippedOrders: PropTypes.array,
};

export default UnshippedOrders;
